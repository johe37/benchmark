# Benchmark

Here is my benchmark to test CPU, memory, disk etc.

## Run

```shell
$ python3 main.py --help
usage: main.py [-h] [--size {small,medium,large}]

Simple system benchmark

options:
  -h, --help            show this help message and exit
  --size {small,medium,large}
```
